# AI Prompts - Примеры промптов использованных при разработке

## Обзор

Этот файл содержит примеры промптов, которые использовались для создания мобильного приложения ZenPulse с помощью AI (Claude Code).

## Промпты для UI компонентов

### 1. PaywallScreen - Экран подписки

```
Создай React Native экран подписки (Paywall) для медитационного приложения.

Требования:
- Премиум дизайн с градиентами (фиолетовый, розовый, золотой)
- SafeAreaView для поддержки устройств с "челкой"
- Список из 6 преимуществ с иконками-эмодзи
- 2 тарифных плана (месячный и годовой)
- Годовой план должен быть выделен как "Самое выгодное"
- Большая кнопка CTA "Попробовать бесплатно 7 дней"
- Адаптивность: на планшетах (width > 768) увеличить все размеры в 1.2-1.5 раза

Используй:
- expo-linear-gradient для градиентов
- react-native-safe-area-context для SafeArea
- useSubscription hook из SubscriptionContext
```

### 2. MeditationCard - Карточка медитации

```
Создай компонент MeditationCard для React Native.

Props:
- title (string) - название медитации
- duration (number) - длительность в минутах
- image (string URL) - ссылка на изображение
- isLocked (boolean) - заблокирована ли медитация
- onPress (function) - обработчик клика

Дизайн:
- Тёмная карточка (#1F1F1F) с закругленными углами
- Изображение сверху (соотношение 16:9)
- Если isLocked=true, показать полупрозрачный overlay с иконкой замка 🔒
- Внизу: название (белый, жирный) и длительность (серый)

Адаптивность:
- На телефонах: ширина 100% минус отступы
- На планшетах: 2 колонки, карточки по (width - 60) / 2
- Использовать Dimensions.get('window')
```

### 3. AI Mood Screen - Экран с AI генерацией

```
Создай экран AIMoodScreen для выбора настроения и генерации аффирмаций.

Функциональность:
1. Показать 3 карточки с настроениями:
   - Радостный (😊) - оранжевый градиент
   - Спокойный (😌) - синий градиент
   - В стрессе (😰) - красный градиент

2. При клике на настроение:
   - Показать лоадер (ActivityIndicator)
   - Имитировать API вызов (setTimeout 2 секунды)
   - Показать аффирмацию в красивой карточке

3. После генерации показать 2 кнопки:
   - "Попробовать снова" (вернуться к выбору)
   - "Вернуться к медитациям" (navigation.goBack())

Адаптивность:
- На телефонах: карточки настроений в колонку
- На планшетах: карточки настроений в ряд
```

## Промпты для исправления проблем

### Проблема 1: SafeArea не работает на iPhone с челкой

```
Исправь проблему с SafeArea в PaywallScreen.

Проблема: контент залезает на челку iPhone.

Решение:
1. Импортировать SafeAreaView из 'react-native-safe-area-context'
2. Обернуть весь контент в SafeAreaView
3. Использовать edges={['top', 'bottom']} prop
4. Убедиться что LinearGradient обернут вокруг SafeAreaView, а не внутри

Пример структуры:
<LinearGradient>
  <SafeAreaView edges={['top', 'bottom']}>
    <ScrollView>
      {content}
    </ScrollView>
  </SafeAreaView>
</LinearGradient>
```

### Проблема 2: Карточки не адаптируются под планшеты

```
Исправь адаптивность MeditationCard для планшетов.

Проблема: на iPad карточки слишком узкие/широкие.

Решение:
1. Добавить константу: const { width } = Dimensions.get('window')
2. Добавить проверку: const isTablet = width > 768
3. Рассчитать ширину карточки:
   const CARD_WIDTH = isTablet ? (width - 60) / 2 : width - 40

4. В MeditationsScreen использовать flexWrap:
   flexDirection: 'row'
   flexWrap: 'wrap'
   justifyContent: isTablet ? 'space-between' : 'center'

5. Каждая карточка должна иметь ширину CARD_WIDTH
```

### Проблема 3: Шрифты слишком маленькие на планшетах

```
Сделай адаптивные размеры шрифтов для всех экранов.

Правила:
- Определить isTablet = width > 768
- Для каждого fontSize использовать тернарный оператор:
  fontSize: isTablet ? 24 : 18  (на ~1.3-1.5 больше для планшетов)

Пример:
const styles = StyleSheet.create({
  title: {
    fontSize: isTablet ? 36 : 28,  // заголовки
    fontWeight: '800',
  },
  body: {
    fontSize: isTablet ? 18 : 16,  // основной текст
  },
  small: {
    fontSize: isTablet ? 16 : 14,  // мелкий текст
  },
});

Применить ко всем экранам: Paywall, Meditations, AIMood
```

## Промпты для навигации

### Настройка React Navigation

```
Настрой React Navigation для приложения.

Требования:
1. Установить пакеты:
   - @react-navigation/native
   - @react-navigation/native-stack
   - react-native-screens
   - react-native-safe-area-context

2. Создать Stack Navigator с экранами:
   - Paywall (начальный экран)
   - Meditations
   - AIMood

3. Настройки навигатора:
   - headerShown: false (убрать заголовок)
   - contentStyle: { backgroundColor: '#1F1F1F' }
   - animation: 'fade' (плавный переход)

4. Обернуть все в SubscriptionProvider

Структура App.js:
<SubscriptionProvider>
  <NavigationContainer>
    <Stack.Navigator>
      {screens}
    </Stack.Navigator>
  </NavigationContainer>
</SubscriptionProvider>
```

## Промпты для Context API

### SubscriptionContext

```
Создай Context для управления подпиской в приложении.

Функциональность:
1. State:
   - isSubscribed (boolean) - статус подписки
   - selectedPlan (string) - 'monthly' или 'yearly'

2. Actions:
   - subscribe() - активировать подписку
   - setSelectedPlan(plan) - выбрать план

3. Создать:
   - SubscriptionContext
   - SubscriptionProvider компонент
   - useSubscription hook

Использование:
const { isSubscribed, subscribe } = useSubscription()

Файл: context/SubscriptionContext.js
```

## Промпты для тестирования

### Тестирование адаптивности

```
Как протестировать адаптивность приложения на разных экранах?

Проверить:
1. iPhone SE (375x667) - самый маленький
2. iPhone 14 Pro Max (428x926) - самый большой телефон
3. iPad Pro (1024x1366) - планшет

Что проверять:
- Все элементы видны полностью
- Текст не обрезается
- Кнопки достаточно большие (минимум 44x44)
- Отступы консистентны
- SafeArea работает
- Градиенты не искажаются

Инструменты:
- Web: Chrome DevTools responsive mode
- Android: Эмулятор с разными AVD
- iOS: Симулятор с разными устройствами
```

## Best Practices для промптов

### ✅ Хорошие промпты:

1. **Специфичные и детальные**
```
Создай компонент PricingCard с props: title, price, period, isPopular, isSelected.
Дизайн: тёмная карточка, если isPopular - золотой бейдж "Самое выгодное",
если isSelected - фиолетовая обводка. Адаптивный под планшеты.
```

2. **С примерами**
```
Стили должны использовать тернарные операторы для адаптивности:
padding: isTablet ? 24 : 20
fontSize: isTablet ? 18 : 16
```

3. **С контекстом**
```
В экране Meditations, при клике на заблокированную медитацию (isLocked=true),
вызвать navigation.navigate('Paywall') для редиректа на экран подписки.
```

### ❌ Плохие промпты:

1. **Слишком общие**
```
Создай экран медитаций
```

2. **Без требований к адаптивности**
```
Сделай карточку с изображением и текстом
```

3. **Без указания библиотек**
```
Добавь градиент
(Какой? CSS, Linear Gradient, другое?)
```

## Типичные проблемы и как их промптить

### 1. AI забыл про SafeArea

**Симптом:** Контент залезает на челку iPhone

**Промпт для исправления:**
```
Добавь SafeAreaView из 'react-native-safe-area-context' с edges={['top', 'bottom']}
вокруг основного контента в [ИМЯ_ЭКРАНА].
```

### 2. AI использовал фиксированные размеры

**Симптом:** Элементы одинакового размера на телефоне и планшете

**Промпт для исправления:**
```
Замени все фиксированные размеры (fontSize, padding, margin) на адаптивные
с использованием тернарного оператора isTablet ? большое : маленькое
```

### 3. AI не добавил numberOfLines

**Симптом:** Длинный текст выходит за границы

**Промпт для исправления:**
```
Добавь numberOfLines={2} и ellipsizeMode="tail" для всех Text компонентов
с длинным контентом
```

### 4. AI использовал неправильную навигацию

**Симптом:** Ошибка "navigation is undefined"

**Промпт для исправления:**
```
Компонент должен получать navigation через props: ({ navigation }) =>
Используй navigation.navigate('ScreenName') для перехода
```

## Заключение

Эти промпты помогли создать полнофункциональное мобильное приложение с:
- ✅ Премиум UI/UX
- ✅ Адаптивным дизайном
- ✅ Правильной навигацией
- ✅ SafeArea поддержкой
- ✅ AI интеграцией

Ключ к успешной работе с AI - давать детальные, специфичные промпты с примерами и контекстом.
